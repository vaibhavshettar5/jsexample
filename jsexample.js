courses{
  python{
    dur:20,start:2,end:4}
  c{
    dur:15,start11,end:12}
}
for(course in courses)
{
  node.buildNode(courses[course]);
  cont.appendChild(node);
}
function build Name(course){
  var html="<h1>"+name+"</h1>"+"<div> <p>"+duration+"</p></div>";
  var box:document.createElement("div");
  return box;
}